# Slack bot token with chat:write and channels:manage (or channels:read + channels:write)
SLACK_BOT_TOKEN=xoxb-your-token

# Optional — used in digest footer links (defaults to http://localhost:3001)
NEXT_PUBLIC_BASE_URL=https://your-findash-deployment.vercel.app
